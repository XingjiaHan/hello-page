name = "hello-page-api"
main = "worker.js"
compatibility_date = "2024-10-01"

[[r2_buckets]]
binding = "MEMES"
bucket_name = "hello-page-memes"

# Optional: rate limiting via KV. Create a KV namespace and paste its id here,
# or leave this block commented out (uploads will work without rate limiting).
# [[kv_namespaces]]
# binding = "RATE"
# id = "<paste your KV namespace id here>"
